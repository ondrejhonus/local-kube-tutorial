volumes:
    - name: config-volume
        configMap:
            name: test-configmap
containers:
    - name: your-container
        volumeMounts:
            - name: config-volume
                mountPath: /etc/config